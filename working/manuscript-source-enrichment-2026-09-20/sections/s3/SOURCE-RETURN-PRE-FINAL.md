# s3 — source reconstruction

**Working checkpoint, not a completed section return.** Owner: `s3-source-enrichment-2026-09-20-connected-session`. This file records substantive recovery already performed. The remaining source reading, composition and reviews are still in progress; `STATUS.json` remains claimed. The completed return will replace this checkpoint, not silently promote it.

## Pinned ground

Repository: `EpiLogos/Antykathera-Essay-Work`. Shared branch: `codex/manuscript-source-enrichment-2026-09-20`. All publication in this lane is confined to `working/manuscript-source-enrichment-2026-09-20/sections/s3/`.

The selected input is `baseline/s3.md`, verified as Git blob `741228ed81f782dba0584616ef546b3db0a48be9`, from `b8f368326a341b68a4ad8bda3c808030d105cbf1:working/manuscript-drafts/s3-mathematical-substrate.md`. It is not the later globally enriched manuscript. The authority/native-source pin is `26e42cbbbaa795ecc7dcc480a10c40393f5a8352`; the earlier fidelity review is read at `832b994cef9480cdfa0abbb08bc01af9fdee068e`.

The wave map, README, AUTHORIAL-DIRECTION and s3 prompt have been read in full. The complete baseline has been read. Its correct treatment of calculus, Boolean delay, iterants, projective charts, cross-ratio, cover/winding, musical ratios and stopping is to be retained, not sacrificed to restored source depth.

## Direct native bodies already recovered

Paths in this section are relative to the repository root. The abbreviation **NDB** below expands to `working/sources-texts-references/epi-logos-plugin-resources-copy-10-07/resources/updated-ql-mef/non-dual-binary/`.

| Source read | Version / coverage |
|---|---|
| `working/sources-texts-references/10-7-2026-core-theorems-pithy.md` | Full I–XI, including the final process-ontology material; blob `180f6c8e1014e7618868899397511f69e4f05480`. |
| `NDB/file-slash/binary-explication-processual-seed.md` | Full, including both tracks, inverse cross-map and final return; blob `1059cd5e82b4515e2cdf85a1b04ad5f12e8c8fe8`. |
| `NDB/canonical-candidate/file-two-processual.md` | Full, including harmonic/fold distinctions and the nested parent table; blob `704a55bdf910dd66d34dbd2b20850d42cb89e94a`. |
| `NDB/canonical-candidate/file-three-quilting.md` | Full six registers and full enantiodromic return/requilting, not only its introduction; blob `9f311a20fa717c42820f377b1e54acfa4b6abaf3`. |
| `NDB/file-zero/binary-explication-current.md` | Full; blob `396b917ba16109985507dea1bf59591be350b6cd`. Directory metadata establishes that `the-binary-explication-v9.md` has this same blob. |
| `working/The_Three_Spanda_Equations_QL_Discovery_and_Harmonisation.md` | Full Parts I–VIII, sections 1–71, Appendices A–D and notes; blob `d92924ef243c7401ef2ca374c8f4238ea1c7ea7d`. |
| `submission-package/essay/symbolon/episteme/concepts/C11-Quaternal-Logic.md` | Full current owner; blob `6fbc180bf1ba14b33a65793120176647dfb41143`. Used as governing mature articulation, not as replacement for the sources above. |

Long reads were taken in overlapping line ranges where the connector truncated its display. The declared full coverage includes those gap-filling reads. A previous agent's coverage statements are not counted as this session's reading.

## 1. What the theorem is doing

The bare relation `−/−` does not start with two already classified objects. Its minimal articulation is knowing and what is known, held distinct without becoming unrelated. Native `X/x` makes capacity and instance readable as one letter in different cases. The Subject is not thereby one of its contents. Nor does the subsequent production of an actual unit manufacture the Subject that knows it. The developmental file-zero language of co-dependency must be read with the mature C11 distinction: the mark depends upon its source for being/coherence, while the source becomes articulated and knowable through the mark. These are different directions of dependence.

The copula develops as `/ = ( = =/≠ ≠ )`. Identity alone eliminates the distance knowing needs; sheer separation makes knowing impossible. The middle relation must retain both identity and difference and must itself be related. Its graphic fourfold and its compressed threefold are distinct readings, not evidence that four and three are numerically equal. The graphic composition of `≠` from slash and equals is a native symbolic construction; it is not, without further specification, a theorem of Boolean functional completeness.

The mature eight determinations remain ordered: `−/− → 0/1 → ?/! → −/+ → X/x → AM/IS → ∞/dx → 1/0`. This traversal is not the whole of QL, an eight-position replacement for the six, or the order of the earlier file-zero's provisional register list. The final section must earn each change rather than display the sequence as a charm. The complete core and the direct supports, not a C01 gloss, are the recovery ground.

## 2. The actual geometric operation

The native station order is centre, north, east, west, south, circumference: `#0`, `#1`, `#2`, `#3`, `#4`, `#5`. Its cardinal angles are north 0°, east 90°, west 270°, south 180°. The east–west passage is an inversion across the diameter. It is not a smooth sequence of four identical quarter-turns.

A transparent worked realisation is to place N=(0,1), E=(1,0), W=(−1,0), S=(0,−1). N, E and W first form a genuine triangle. Adding S enlarges the enclosing boundary to the square N–E–S–W–N. The old E–W edge remains as an internal diagonal: it has changed office without being abolished. Acquisition order and boundary order are not the same operation. Joining the four points in acquisition order would not give that square, so the prose must not pretend it would.

The triangle's internal angles total 180°; the square's total 360°. Their areas in this realisation are 1 and 2. The ratio of their numbers of sides is 4/3. These are three different comparisons. The sources' 75% figure belongs to a count/proportion reading, not to a claim that the selected N–E–W–S route is a 270° perimeter walk or that 180° is three quarters of 360°.

The personed reading is prior to the musical and physical reframings: first-, second- and third-person articulation is encountered from within an awareness that can recognise their shared field. Context is not an omniscient fourth observer outside the figure. The change from a valid bounded perspective to recognition of its placing field is the operation that later geometries inherit. The old perspective is retained as a true local disclosure; only its claim to exhaust the field is lost.

## 3. Inverse phase: placement is not content

The outer slash-form table is `#1=0/0`, `#2=0/1`, `#3=1/0`, `#4=1/1`, framed by the two single poles. Its folds pair `(0,5)`, `(1,4)`, `(2,3)`; its harmonics pair `(0,1)`, `(2,3)`, `(4,5)`. Sharing the middle pair does not make folds and harmonics synonymous.

The full parent `P=(0/1)/(1/0)` has child contents, in positions 1–4, `(0/1, 0/0, 1/1, 1/0)`. Its return parent `P′=(1/0)/(0/1)` has `(1/0, 1/1, 0/0, 0/1)`. Both retain a complete four-plus-two. The implicate is not an empty label around four active items.

The key restoration is the source's cross-reading of positional and content office. The opposed child pair occupying the structural places is read as `0/1` with `1/0`, which the outer content table recognises as the mixed/formal pair. Conversely, the middle child pair is `0/0` with `1/1`, the pure/structural contents. The forward and return parents supply the two orientations of that exchange. Structure and Formality exchange office while Essence remains the axis.

This must not be reduced to the permutation reversing four words. That reversal by itself preserves the pure and mixed sets. The office exchange appears only when the inherited positions and the actual contents are explicitly cross-read at their respective levels. Similarly, the single poles swap within the nested parent expansion, while the fixed-position signed register can retain bare minus and bare plus. These are different readings, not an unnoticed contradiction to be concealed.

## 4. First Spanda: restore the constructive middle

The original core/seed bridge is `(1/0 + 0/1)`. The later Discovery document deliberately writes `∨` to prevent ordinary fraction addition from being inferred. The baseline's replacement with an unexplained `⊕_QL` is not a substitute for recovering this development.

The whole movement is:

`0 → 0/0 → (0/1)/(1/0) → T0 → T1 → (1/0 + 0/1) → 1/1 → 1 = 100%`.

The nested relations are load-bearing. In the First-Spanda naming, T0 is the emanative `0/(0/1)` toward `0/1`; T1 is the reversionary `(1/0)/0` toward `1/0`. The ratio-of-ratios makes the two oriented relations themselves terms. None of these zero-denominator forms is being evaluated as an ordinary real fraction.

The later construction has several distinct exact carriers. The walking isomorphism has two objects and four arrows: the two identities and two inverse cross-arrows. It is one connected component without identifying its objects. A delayed Boolean equation `s(t+1)=1−s(t)` produces ordered windows `01` and `10`. Their componentwise join is `11`; a declared change of register reads that as `1/1`, then as unit ratio. The bit word `11` is not being evaluated as the base-two integer three and then equated to one.

The vector construction makes the retained difference explicit. With b=(0,1), p=(1,0), swap X, u=(1,1) and d=(1,−1), we have Xu=u, Xd=−d, b=(u−d)/2 and p=(u+d)/2. The invariant and the sign-changing channel jointly reconstruct the phases. The join output alone is not a lossless record of the inputs; the difference/reference has to remain available. Adding polarity Z gives J=XZ and J²=−I. Oscillation, invariant extraction and rotation are therefore related but non-identical operations.

The actual One is the achieved invariant, distinguished from the affirmative pole already in the alphabet. Its later change from satisfaction to next-cycle ground is a change of recursive role, not `1=0` in arithmetic and not a proof that consciousness is generated by bit operations.

## 5. Second and Third Spanda: the account is typed

For a six-place frame P, binary configurations form B^P with 64 members; ordered local addresses form P×P with 36. Their disjoint union has 100 members. Normalising this account as a percentage is an additional declared act: the cardinal 100 and unit 100% are not the same quantity.

Partition P=A⊔B with three positions each. Then P×P has four directional blocks of nine. The binary configuration space factorises as two eight-member triad spaces. The ratio of the two account sectors is 64/36=16/9=2^4/3². Reading four binary factors and two ternary factors recovers the four-plus-two interpretation; it does not assert 16/9=6. Dividing the sum 64+36 by four instead gives 25, not 16/9.

The Third Spanda retains the actual unit and phase-labels the entire ordered-relation sheet: `{actual} ⊔ B^P ⊔ (C2×P×P)` has `1+64+72=137` members. The local arrow direction was already counted in P×P. The extra factor two records whole-sheet phase. Complementing binary configurations permutes the existing 64, so it does not require a second disjoint state-space. The exact harmonic transfer is `36 ×16/9 =64`, then `64 ×9/8 =72`; the product ratio is 2.

The wider decimal, vortex and symbolic discovery field has been read in full. It belongs in the recovery record, but its many exact identities and historical receptions do not need to crowd the six-movement essay. In particular, a near-137 physical constant is not an empirical derivation from this finite set model.

## 6. Mature corrections retained while recovery continues

A torus's four presentation-side occurrences plus two generator classes are a typed presentation accounting, not six cells of a CW decomposition. The infinite plane covering the torus is not the two-sheeted torus covering of the Klein bottle. A surface or atlas supplies neither a time law nor an attractor by itself. Endpoint return does not store winding history without a path/lift/history record.

Likewise, exact 9/8, the Pythagorean comma, a tempered whole-tone step, a standing-wave eigenmode and a chosen ending are not one interchangeable 'remainder'. Native musical return must reach actual resolution and possible silence, not impose perpetual unfinishedness as a philosophical requirement.

## Outstanding work at this checkpoint

The remaining direct supports—Self-Proving Self, full musical derivation and Definition of God Draft 3—still require this session's full reading. Relevant original quilt/phone/paper interventions, the mature argument owners and their conjugates, complete source-house checks and neighbour seams remain to be recovered. No claim of completed all-source coverage, completed literary composition, independent review, executable repository-harness success or ready handoff is made by this checkpoint.
