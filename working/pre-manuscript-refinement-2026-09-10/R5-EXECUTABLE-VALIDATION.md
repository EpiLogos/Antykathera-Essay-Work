# R5 — Executable validation receipt

**Standing:** T25/R5 closure evidence. This receipt records the cold-review executable gate after semantic repair; it is not T26 ratification and it does not authorise manuscript prose.

## Semantic prerequisite

The independent semantic review is recorded in [R5-COLD-SEMANTIC-REVIEW.md](R5-COLD-SEMANTIC-REVIEW.md). It read all eight stations / forty-eight movement bodies in order and reread the load-bearing canonical owners without taking R3/R4 verdicts as evidence for themselves.

Eight bounded semantic faults were repaired before executable acceptance:

1. M11's stale Actuation consumer title;
2. M17's over-identification of a division-by-zero witness with the transcendental immutable gap;
3. M18's misdescription of polar dia as using an external zero;
4. M22's `mythic proof-case` wording;
5. M31's overstatement that the formal Arche-Topos derives the psychoid extension;
6. A/C's false claim that `2 = 0/1` is standard set-theoretic notation;
7. S's stale statement that the Abhinavagupta/Bhāskara chronology was still uncollated;
8. A36's mislabelling of A02/Copula as the formal-limit owner instead of A03.

The repaired seams were reread before the executable pass. No compensating semantic contradiction was found.

## G1–G10 and comparative/negation audit

The cold-review faults map to the active WRITING-PROTOCOL gates rather than to a style clean-up campaign:

- **G1 / warranted claim strength:** M17, M31 and the A/C set-theory floor were repaired where a comparison or local formal result had been made stronger than its warrant.
- **G3/G5/G8 / dependencies, referents and seams:** M18 was repaired so the §1 release actually prepares the positive polar-dia distinction developed in §2; M11 and A36 were repaired where a stable consumer/owner had been misnamed.
- **Comparative/negation gate:** M17 keeps mathematics distinct from transcendental argument; M22 keeps Mytheme as test/enactment rather than proof; M31 keeps formal derivation distinct from psychoid extension; A/C keeps von Neumann construction distinct from native QL notation.
- **G2/G4/G6/G9/G10:** the cold whole-flow read produced no additional named fault requiring calibration. The calibration corpora therefore remained closed; no voice or style example was used to manufacture a repair.

## Initial executable diagnostic

A read-only diagnostic was run from the semantically repaired tree using the real repository tools. Its purpose was to discover the current contract rather than assume that pre-S tests still described it.

The first diagnostic established two separate facts:

- all eight room-depth audits passed;
- the repository suite and generated reader contract still contained pre-S assumptions.

The initial full suite result was:

```text
Ran 115 tests in 391.766s
FAILED (failures=8, skipped=1)
```

The eight failures were dispositioned as follows:

1. navigation knew an `episteme-products` class had been requested but lacked a complete class/index contract;
2. M26 contained a real harmonic warrant but the quality contract could not see an explicit warrant surface;
3. M39 context tests still expected the superseded J-Space/A14 sovereign route rather than the AIKit/product-aware context;
4. substantive search still expected `circumscription without circumstance` in old M39 instead of its preserved home in S1 Actuation;
5. seven visible reader dead ends remained: the Genesis/Paradigm whole and six newly added source houses;
6. the publication architecture test still denied the real `products/` Episteme domain;
7. the submission census test still expected 181 source houses rather than the current 187;
8. the Agentworld matrix contained one bad relative link to the Genesis/Paradigm whole.

The single skip was the existing AIKit parity class because the `aikit` binary is not installed in the Actions runner. It is an environment/runtime limit, not evidence that AIKit parity passed.

## Integration repair

The permanent repair preserves the historical baselines rather than rewriting them:

- the September 9 **281-record** acceptance receipt remains historical and unchanged;
- the live T25 admission is reproducibly derived as **288 records** at `T25-current-census-acceptance.json`;
- the philosophical shared field remains the distinct **144-record** `36 A + 36 A′ + 64 C + A/C + 7 S` composition.

The live reader now binds the T25 receipt. Products/product-field are first-class Episteme navigation and canonical-graph node types. Their declared `argument_relations`, `concept_relations`, `product_parent` and `members` participate in bounded context/effects traversal. The six new source houses and the Genesis/Paradigm whole now expose truthful returns to their actual movement consumers. The Agentworld relative link and M26 warrant surface were repaired. Tests were updated only where the commissioned S family changed the actual architecture.

The bounded integration proof passed before publication of the integration commit:

```text
Ran 45 tests in 251.813s
OK
```

Reader proof at that gate:

```text
admitted: 288
missing_admitted: 0
unreachable_admitted: 0
visible dead ends: []
```

The validated integration landed at commit `c665bd906464f41b2544767010f49e7e6d37942a`.

## Final full validation

A fresh read-only validation was then run from the committed integrated tree plus only the validation-workflow commit. No generated or canonical content was changed by this gate.

Commands/checks:

```text
python3 tools/build-t25-refinement-admission.py --check
python3 tools/build-source-projections.py --check
python3 tools/build-section-rooms.py --check
python3 tools/build-navigation.py --check
python3 tools/audit-reader-navigation.py --project-root . --output <temp>/r5-reader.json
python3 -m unittest discover -s tests -v
python3 tools/audit-room-depth.py --project-root . --room <all eight rooms>
git diff --check
```

All generator freshness checks passed.

Reader state:

```text
pages: 782
reachable: 718
admitted: 288
missing_admitted: 0
unreachable_admitted: 0
visible dead ends: []
```

The reader audit still reports broader portability diagnostics outside the blocking admitted/return-route gate:

```text
directory-link: 6
title-or-alias-only: 505
missing-vault-path: 91
ambiguous-filename: 5
missing-fragment: 3
missing-path: 1
```

These are retained as portability/audit diagnostics. They are **not** being renamed as absent: they remain visible in the audit output. The present R5 gate establishes that all 288 admitted records are present and reader-reachable and that no non-protected public page lacks a visible route back to the movements; it does not claim the broader portability diagnostic counters are zero.

Full repository suite:

```text
Ran 115 tests in 370.747s
OK (skipped=1)
```

The one skip is the AIKit parity class because `aikit` is unavailable in the runner environment. No AIKit whole-feature proof is claimed from that skip.

All eight room-depth audits passed:

```text
PASS 00-integral-threshold
PASS 01-differentiating-mind
PASS 02-return-of-zero
PASS 03-two-logics
PASS 04-mathematical-substrate
PASS 05-psychoid-flowering
PASS 06-objective-internality
PASS 07-instrument-returns
```

## Preservation anchors

The final validation checked the protected anchors directly:

- sovereign manuscript `submission-package/essay/THE-RETURN-OF-ZERO.md` blob: `df84af01672eecb57ec6e17aed416cb0045cfe62` — unchanged from the pre-refinement base;
- canonical exact survivor `S1-Actuation.md` blob: `c3bc08715693fda275ccb1b9be8a318e1f12303d`;
- retained `S1-Actuation.md.original` blob: `c3bc08715693fda275ccb1b9be8a318e1f12303d`.

The frozen A/C ethic remains referenced rather than agent-rewritten. Protected authorial NOTES/AUTHORIAL-TEXT and the sovereign manuscript were not used as repair surfaces.

## R5 disposition

**R5 passes.** The refined whole has survived an independent semantic read and the real executable/readability gate. The remaining portability diagnostics and missing AIKit runtime are recorded honestly and do not support stronger claims than the checks performed.

The next work is R6: assemble the evidence-bearing T25 return, exact register-envelope/tone table and the proposed T26 common writing base for Frank's explicit ratification. R5 does not ratify that base and does not begin manuscript prose.
