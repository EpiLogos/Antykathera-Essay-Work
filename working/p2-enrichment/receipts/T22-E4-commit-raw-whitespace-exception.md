# Raw witness whitespace exception

All approved manifest SHA-256 hashes match before commit. Git whitespace check reports inherited formatting in downloaded original HTML (trailing whitespace, CRLF and indentation). These source witnesses remain byte-for-byte unchanged. Scoped whitespace check excluding only the ten original HTML witnesses passes for all authored SOURCE/proposal/proof files. No parser, source or T22 test is bypassed.
