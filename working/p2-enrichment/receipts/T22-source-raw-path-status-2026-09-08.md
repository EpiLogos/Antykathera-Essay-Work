# T22 — Irenaeus / Job raw provenance paths and CSM passage status

Completed 2026-09-08. Metadata/retrieval repair only; no new research or argument.

- Irenaeus: converted 67 acquisition href occurrences for 24 unique retained objects into labelled literal repository paths. Reused the unchanged full-read SOURCE hash from T20-valentinian-source-acquisition; all 22 HTML objects, the full selected-text extraction and acquisition manifest independently match recorded SHA-256 values.
- Job: converted the one complete-PDF acquisition href into a labelled literal repository path. The 361689-byte PDF matches its recorded SHA-256. All 42-chapter scope, eight passage cards, quotation boundaries and consumer metadata remain unchanged.
- CSM: added three standalone `**Status:** authorial-provenance-only; never-evidence; not quotation evidence.` fields. Original inline statuses, quotes, locators, provenance, false citation readiness and never-evidence standing remain intact. The previously disclosed q002 transcription difference is not altered or authenticated.

Inverse reconstruction verifies that removing only the authorized replacements/additions recovers each before-SOURCE byte for byte. All 25 raw objects remain unchanged. Effects were read before writing; no declared transverse threads. No NOTES exists beside these three houses; no protected or consumer files were changed.

Validation: Irenaeus/Job scoped T22 **2 targets / 0 failures**; CSM scoped T22 **1 target / 0 failures**. Actual source parser: **536 passages**, including Irenaeus 43, Job 8 and CSM 3; all three CSM statuses recognized. Doctor reports **0 missing-passage-status** debts. Its unrelated global debts are retained separately in the proof; aggregate projection/BKMR rebuild remains parent-owned.

Proofs and before snapshots: `working/p2-enrichment/snapshots/T22-private-source-raw-path-status-2026-09-08/`. `change-proof.json` carries exact canonical paths, before/after hashes, every conversion and all raw-object hashes. `explicit-owned-paths.txt` bounds the authorized commit. No raw files were modified or newly acquired. No outstanding issue remains in this scoped batch.
