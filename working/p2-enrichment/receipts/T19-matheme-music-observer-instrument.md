# T19 — matheme-music-observer-instrument

Canonical scope: only `submission-package/essay/symbolon/matheme/music/observer-instrument.md`. Recovered matching packet and actual File Four §5→0 observer/performance/recognition close. Musical-v3 lineage, native core, four-file context, register contracts/Two Locks, source houses and A06/A15/A17/M29 were recovered in preceding assignments. No other writer’s draft or canonical page edited. Bounded material-bearing recovery is recorded, not a fresh full-read claim for every wide packet dependency. Parent’s native-source effects preflight applies.

## Operation and audit

Sixfold follows score versus sounding, four observer surfaces, frequency-to-address calculation, specified played loop/re-anchor, verification obligations, and cyclic/telic return. It develops the source’s actual observer office with an executable mathematical example, without constructing an imagined current runtime.

G1/G5: intended sequence, signal and interpretation remain distinct. FFT/STFT, CQT, chromagram and cymatic observation retain their actual data objects. A spectral component is not automatically a separate played note; aggregated chroma is not ordered cadence evidence. Physical nodes require an actual material system or stated physical model. No 8+4-to-physical-node identity or protected-memory implementation is asserted.

G2/G6: the nearest-class formula names its C reference and rounding boundary. Independent reread found a malformed nu glyph in the display; repaired it to match the operation actually used and checked. Expanded CQT as constant-Q transform. Exact octave-equivalent classes are computed on constructed frequencies; these calculations are explicitly not acoustic capture. The comma example begins at an exact bin centre, avoiding a false universal claim that small frequency changes can never cross a quantisation boundary.

G3/G5: the G–B–D to C–E–G example separates a harmonic realisation from the individual CF5/CF1 degree choices. Re-anchoring at D changes coordinates while retaining the signal. CF7’s B-to-next-C, same-position pulses and complement paths keep their distinct position/face/register operations. The observed whole would need temporal and harmonic context, not merely pooled class counts.

G7/G9: removed the source’s universal “no local disturbance” toric-code description rather than repeating it as observer warrant. Kept the separate formal-neighbour route and the requirement to verify this instrument through its own operations. The source’s claim that every performance ends an epogdoon short is replaced with the actual 16/9×9/8=2 return; completed performance and telic recognition retain their proper offices.

Two Locks: full chain retained; inverse-phase process primes, Night/conjugate instrument primes and octave registers distinguished. Standing identity keeps its native directed-reading office. Current ql-mef implementation is unrecovered; no live run, audio capture or phenomenal transformation is certified. The enacted relation retains Derived native force while acoustic/runtime verification remains Open.

## Verification

`T19-matheme-music-observer-instrument-check.py` computes all twelve constructed frequencies, their classes and octave returns, the comma’s 23.46-cent size and bin-centre invariance, and the written cadence/re-anchor sets. All pass after revision. The JSON explicitly identifies these as constructed mathematical inputs, not recorded sound or runtime output. Saved in `T19-matheme-music-observer-instrument-arithmetic.json`.

Real workspace link check: 13/13 resolved; `T19-matheme-music-observer-instrument-links.json`. Parent owns shared hygiene/census backcheck.

Final SHA256: `eec1824c84b514d5faf6ca89450e0e3dde1f66208cae2778f6fdd8359ef70b34`.
