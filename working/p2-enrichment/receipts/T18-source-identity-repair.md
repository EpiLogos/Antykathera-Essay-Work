# T18 source identity repair — 2026-09-07

The root barrier exposed three unresolved `source_ids` edges for the newly housed Binary Explication. Added `source_id` equal to its existing `record_id`. All three edges then resolved; root batch: 13 targets, 197 edges, zero unresolved links. The report retains original doctor/effect results and records the targeted recheck.

Projection regeneration required the same exact-ID addition to the new CSM lecture-note house. All source houses now have source IDs. Its effects were recovered at `/tmp/T18-csm-identity-effects.json`. Projection parsing also required quotation of two existing Levinas Totality and Infinity title scalars containing colons; values were preserved. No body, attribution, authority, readiness, NOTES or locality change was made.

`build-source-projections.py` and `--check` now reach relation validation but fail on existing unresolvable `main_source_for` labels in Descartes, the core-theorem house, and the two Levinas houses. No relation semantics were silently rewritten. This remaining projection-generation debt does not change the independently passed root local-link gate. Parent owns input hash refresh after these metadata changes.
