# T22 — Job intended consumer

The acquired source declared an explanatory string under `main_source_for` before its intended Mytheme existed. The real projection command rejected this unresolved relation. Preserve `intended_consumers: [mytheme-job]` and the full source content; leave the current resolvable relation list empty until the actual whole is written, then bind its exact path. No source readiness or narrative claim changes. This is a relation-state repair, not a missing-whole placeholder.

The whole now exists and has a direct source-house body link. Inspection of the real projection builder shows `main_source_for` is specifically a section-code field, not a general canonical-consumer field. Keep that list empty rather than place a valid whole path in the wrong schema slot. The body relation proves actual whole consumption; the intended-consumer field retains its admitted identity.
