# Protocol Reconciliation — Pass B Close

**Ticket:** 014  
**State:** reconciled pre-T07 protocol  
**Pass-A provenance:** `PROTOCOL-RECONCILIATION-PASS-A.md` preserves the detailed source-by-source contradiction table.

## 1. Surviving authority rule

Repository instructions locate and execute the authored world; they do not derive it. The current working order is:

`AGENTS.md → repository locator/protocol → central plan → orienting principles → register contracts → ratified quilt/census decisions → local skills/tools`.

Where a lower layer conflicts with a higher layer, repair the lower layer rather than carrying two executable interpretations.

## 2. QL page law — closed

Every developed field page begins from:

```markdown
# <record title>

## #0

## #1

## #2

## #3

## #4

## #5→0
```

There is no universal semantic mapping of these positions. Local proposition/image/operation, register, record type, inherited/outgoing relation, Return, publication office and QL foundational qualities determine their meaning. Descriptive subtitles are authored locally only when earned.

`return-of-zero-pages` is already compliant. O:I/AIKit rendering **does not alter this law**: a renderer may expose six structural positions, but may not infer universal labels.

## 3. Census/build law — closed

The only valid build direction is:

`complete quilt → harmonisation → whole-field census → T07 ratification → T08/P1 propagation → source lock → deterministic build`.

- Old counts are historical facts, never intake truth.
- A recovered note is not automatically a concept.
- A filename is not proof of a unique operation.
- Generated intake/headings are discovery aids only.
- Every ratified record or explicit non-page carrier must be bijectively accountable to the ratified census.

`return-of-zero-build` is compliant.

## 4. Link law — closed

A written link creates an authored relation; navigation exposes it. A page has exactly the relations its operation requires. There is **no numeric degree quota**.

`return-of-zero-links` was repaired in Pass A. Authored, mechanical, inferred, learned, QL-derived and MEF-derived graph edges remain distinguishable.

## 5. Register / record-type / office law — closed

These are separate axes:

- register = how the record knows/performs;
- `record_type` = what publication object it is;
- publication office = LINEAR-BEARING / SUPPORTING-VISIBLE / RADIAL-DEPTH / REDUNDANT-WITH-NAMED-CARRIER.

A root relation, linear argument and Matheme derivation may concern the same relation while performing different work. Link them; do not copy one body into three files.

## 6. Source / identity / Projection law — closed

The Wiki contract introduces no protocol contradiction. It makes existing one-home law more explicit:

- canonical Symbolon source identity/home remains source-owned;
- source revision ≠ Projection revision;
- O:I Projection is a selective representation, not transfer of authority;
- browser/agent edits remain Projection revisions or return proposals until accepted through the source process;
- explicit acceptance creates a new source revision; otherwise the Projection remains divergent/local;
- AIKit's target-capability projection is a different operation and must not be conflated by naming.

## 7. Whole-level O:I distinctions — closed

The protocol must preserve:

`SharedField ≠ WikiSpace ≠ Context`

and

`Wiki relation ≠ Contribution relation ≠ learned KnowledgeRoute`.

Knowledge Navigation is federation, not a universal graph. Provider/source identity and authority remain native. `frame` is a derived operative pack, not canonical ContextSource. `route` records traversal and does not mutate provider graphs.

## 8. Technical-source law — closed

Current product claims must carry exact repository revision and claim role: intent, accepted architecture, implementation, experiment/research. Product code can demonstrate a seam or pressure a software correspondence; it cannot retroactively derive the essay's ballein/QL/metaphysical structure.

The revalidated source map is the evidence lock for 012/013. Later T16 rechecks drift before public source lock.

## 9. Historical locator files

Two factual locators intentionally remain stale until T07 because changing them now would make a proposal appear canonical:

- `docs/REPOSITORY-SHAPE.md` old inventory language;
- `submission-package/essay/symbolon/README.md` former seven-root proposal.

Their **surviving rule is already unambiguous**: counts/root lists are historical candidate state. T08/P1 updates them to the ratified/post-propagation field.

## 10. Fresh-session invariants

A fresh execution must recover all of these:

1. dia/sym-ballein and formal-limit/process foundations precede O:I;
2. raw QL chassis has local semantics only;
3. register ≠ record type ≠ publication office;
4. one unique operation has one primary home; related registers link rather than duplicate;
5. historical counts never constrain census/build;
6. every recovered note has an explicit carrier;
7. no numerical link quota;
8. source evidence and conceptual work keep separate homes;
9. build consumes the ratified census;
10. source identity ≠ Projection identity ≠ renderer/provider id;
11. SharedField, WikiSpace and Context remain distinct;
12. Wiki relation, Contribution and KnowledgeRoute remain distinct;
13. O:I product position is responsibility/research, not chronology or QL proof;
14. technical implementation can operationalise/test/pressure but not prove the essay's metaphysics.

No known executable protocol now contains an unresolved contradiction capable of producing a different field structure in a fresh session. Remaining stale locator text is explicitly non-governing and docketed for post-T07 propagation.
