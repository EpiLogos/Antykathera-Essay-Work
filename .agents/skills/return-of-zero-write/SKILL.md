---
name: return-of-zero-write
description: Use when exploring, composing, pair-writing, drafting, revising, or teaching prose for The Return of Zero or its section working surfaces.
---

# Write The Return of Zero

Conversation and unfinished exploration may remain in chat. Retrieve canon when it begins to bear the thought; do not force an exploratory exchange into a file destination.

For manuscript work, open the exact movement and the sovereign master manuscript at `submission-package/essay/THE-RETURN-OF-ZERO.md`. Use `python3 tools/project-agent-harness.py writing-context <movement-id> --json` for a compact canonical packet, then reopen every load-bearing path. Movements live in the rooms (`submission-package/essay/section-rooms/<room>/movements/`), which are the publication `#0`; the argument field (`section-rooms/arguments/`) is their implicate `0`. A generated `ROOM.md` is a waypoint. `READING.md` is a protected learning route; `SCRATCH.md` is an optional temporary writing pad; `VISUALS.md` exists only for an admitted visual argument.

Frank’s established proposition governs. Sources deepen, historicise, test, and articulate it; they do not grant it permission to exist. Preserve a qualification only when a concrete source, inference, attribution, or canonically live tension changes the movement. Scope the claim; do not soften it into a safer neighbour or manufacture counterpressure.

Depth records declare their register before their content: the record opens by naming the proposition or object it serves and the register in which that proposition operates (matheme, mytheme, episteme, or a declared cross-register composition). The route of return names the register boundary it crosses back into the essay or argument — a derivation does not return as an image, and an image does not return as proof.

Before authored prose, read `writing-guidance-tools/README.md` and follow the load contract in `writing-guidance-tools/SKILL.md`: draft with only `references/WRITING-LAWS.md` and `references/WRITING-RUBRIC.md` open, audit independently, and keep calibration files cold until after that audit. Use fresh-eyes and ship-note checks at the scale of the passage, not as a universal completion ritual.
